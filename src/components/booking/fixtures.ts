import type { Slot } from './types';

/**
 * Fixture slots for developing and testing the calendar before the backend exists.
 *
 * Cover every state: available, filling up, full, registration closed, and past. Include
 * days with no slots at all, and at least one slot that falls on a different calendar day
 * in another timezone — that is where timezone bugs surface.
 */

export const fixtureSlots: Slot[] = [
  // TODO: add fixtures
];
